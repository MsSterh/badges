<h1>Add new item for "<%= @collection.title %>" collection</h1>

<%= form_for [@collection, @item], url: new_collection_item_url(@collection.id) do |f| %>
  <%= f.text_field :title %>
  <%= f.submit :create %>
<% end %>
